// interface with the methods for editing an auto

package adapter;

public interface EditThread {
	// calls an instacne of the EditOptions class to start the cycle of getting and setting
	public void update(String modelname);
}